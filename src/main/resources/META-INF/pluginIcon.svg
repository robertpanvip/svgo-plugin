<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40">
    <!-- 深色圆背景，确保鲜艳元素突出 -->
    <circle cx="20" cy="20" r="19" fill="url(#paint0_linear)" />

    <!-- 高饱和度鲜艳渐变定义 -->
    <defs>
        <!-- 显示器主体：亮蓝 → 青绿渐变（超级鲜艳） -->
        <linearGradient id="bodyGrad" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#00E0FF"/>
            <stop offset="100%" stop-color="#00FFAA"/>
        </linearGradient>

        <!-- 预览区方块：紫 → 粉渐变（活力感强） -->
        <linearGradient id="previewGrad" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#FF6BFF"/>
            <stop offset="100%" stop-color="#9D4EDD"/>
        </linearGradient>
        <linearGradient id="paint0_linear" x1="2.94192" y1="4.89955" x2="37.7772" y2="39.7345" gradientUnits="userSpaceOnUse">
            <stop offset="0.15937" stop-color="#3BEA62"/>
            <stop offset="0.5404" stop-color="#3C99CC"/>
            <stop offset="0.93739" stop-color="#6B57FF"/>
        </linearGradient>
    </defs>

    <!-- 放大后的显示器主体（更大、更抢眼） -->
    <rect x="5" y="7" width="30" height="22" rx="4" fill="url(#bodyGrad)" stroke="#00FFFF" stroke-width="1"/>

    <!-- 屏幕（深色，突出内容） -->
    <rect x="7" y="9" width="26" height="18" rx="2" fill="#3C99CC"/>

    <!-- 左侧代码：粗亮绿竖线（更醒目） -->
    <line x1="13" y1="12" x2="13" y2="24" stroke="#39FF14" stroke-width="3" stroke-linecap="round"/>
    <line x1="17" y1="12" x2="17" y2="24" stroke="#39FF14" stroke-width="3" stroke-linecap="round"/>
    <!-- 右侧预览区：鲜艳紫粉渐变方块（更大） -->
    <rect x="22" y="14" width="9" height="9" rx="2" fill="url(#previewGrad)"/>

    <!-- 大眼睛（白色高光，更突出“预览”） -->
    <circle cx="26.5" cy="18.5" r="3" fill="none" stroke="#FFFFFF" stroke-width="2"/>
    <circle cx="27.8" cy="17.2" r="1.5" fill="#FFFFFF"/>

    <!-- 底座（灰色，增加立体感） -->
    <rect x="14" y="29" width="12" height="3" rx="1.5" fill="#39FF14"/>
    <rect x="12" y="32" width="16" height="2" rx="1" fill="#39FF14"/>
</svg>